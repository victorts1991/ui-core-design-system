import type { Preview } from "@storybook/react";

// Import directly the CSS file where Tailwind is defined
import "../src/styles/global.css";

const preview: Preview = {
  parameters: {
    controls: {
      matchers: {
        color: /(background|color)$/i,
        date: /Date$/i,
      },
    },
  },
};

export default preview;